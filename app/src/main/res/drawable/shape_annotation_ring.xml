<?xml version="1.0" encoding="utf-8"?>
<!-- Drawn over the chosen colour of the palette. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@android:color/transparent" />
    <stroke
        android:width="3dp"
        android:color="@color/section_header" />
    <size
        android:width="36dp"
        android:height="36dp" />
</shape>
